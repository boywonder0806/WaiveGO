# packages/shared — Shared Code

TypeScript types, constants, and utils shared between `apps/web` and `services/api` (e.g. the
shape of a check-in result, Smartwaiver record types, API request/response types).

Not yet scaffolded — will become an npm workspace package once `apps/web` and `services/api`
exist and need to share types.
