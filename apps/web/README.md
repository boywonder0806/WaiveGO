# apps/web — Staff/Admin Dashboard

Stack: **Next.js + TypeScript**.

Staff-facing web dashboard: live check-in activity, waiver/verification status, guest lookup,
and exception handling (e.g. face matched but no signed waiver).

Not yet scaffolded. Next step: `npx create-next-app@latest .` (TypeScript, App Router) run from
this directory.
